{
  "private": true,
  "scripts": {
    "lint": "lerna run lint --stream",
    "format": "lerna run format --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "publishConfig": {
    "registry": " https://registry.npmjs.org"
  },
  "workspaces": [
    "packages/*"
  ]
}
