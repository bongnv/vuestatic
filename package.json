{
  "private": true,
  "scripts": {
    "lint": "lerna run lint --stream",
    "format": "lerna run format --stream",
    "watch": "lerna run watch --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
