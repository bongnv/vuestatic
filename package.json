{
  "private": true,
  "scripts": {
    "lint": "lerna run lint --stream",
    "format": "lerna run format --stream"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
