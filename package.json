{
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "lint": "lerna run lint --stream",
    "build": "lerna run build --stream",
    "watch": "lerna run watch --stream --no-sort",
    "test": "lerna run --stream test",
    "test:clearCache": "lerna run --stream test -- --clearCache",
    "test:updateSnapshot": "lerna run --stream test -- -u"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
